<%= form_for(@exam) do |f| %>
  <% if @exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% @exam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
	<tr><td>Title: &nbsp;</td><td><div class="field"><%= f.text_field :content %></div></td></tr>
	<tr><td>Date</td><td>  <div class="field"><%= f.text_field :examday %></div></td></tr>
</table>

<br>

<br>
<!--
  <div class="field">
    <%= f.label :objective_id %><br>
    <%= f.number_field :objective_id %>
  </div>
  <div class="field">
    <%= f.label :stem_id %><br>
    <%= f.number_field :stem_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
-->
<% end %>
