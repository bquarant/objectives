<%= simple_form_for(@stem) do |f| %>
  <% if @stem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stem.errors.count, "error") %> prohibited this stem from being saved:</h2>

      <ul>
      <% @stem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table class="table">
	<tr><td><strong>Exam</strong></td><td><%= f.number_field :exam_id %></td></tr>
	This should be a dropdown box of Exams...
	<tr><td><strong>Question stem</strong></td><td><%= f.text_field :content %></td></tr>
	<tr></tr>
</table>
<br>
<center>
<%= f.button :submit, "Create", :class => "btn btn-primary" %>
</center>
<br>

<% end %>
